{
  "name": "afazi-terapi",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "web": "python3 -m http.server 8080 --directory www",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.9.2"
  }
}